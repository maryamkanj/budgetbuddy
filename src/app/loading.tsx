import { LoadingFallback } from '@/components/common/LoadingFallback';

export default function Loading() {
    return <LoadingFallback title="Loading..." description="Please wait while we prepare your space." />;
}
